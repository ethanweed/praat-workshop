
# open the orginal sound file
Read from file: "/Users/ethan/Documents/GitHub/praat-workshop/cardinal_vowels.wav"

# select the sound object
selectObject: "Sound cardinal_vowels"

# get the intensity (for identifying areas with silence)
To Intensity: 100, 0, "no"

# make a TextGrid object (to mark areas with silence and sound
To TextGrid (silences): -25, 0.1, 0.05, "silent", "sounding"

# select the sound and TextGrid objects at the same time
selectObject: "Sound cardinal_vowels"
plusObject: "TextGrid cardinal_vowels"

# tell Praat to use the TextGrid information to divide up the sound file
Extract intervals where: 1, "no", "contains", "sounding"

# make a new folder to put the seperate files in
createFolder: "/Users/ethan/Desktop/cardinal_vowels"

# loop through the new files and save them to the folder

# count how many files to loop through (they are already selected at this point)
n = numberOfSelected("Sound")

# loop through the sound objects and save them to the new folder as WAV files
for i to n
    soundObject[i] = selected("Sound", i)
    Save as WAV file: "/Users/ethan/Desktop/cardinal_vowels/cardinal_vowels_sounding_" + string$ (i) + ".wav"
endfor

